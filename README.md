# SMS-Expense-Tracker
SMS Expense Tracker is an Android-based expense management system that captures transaction SMS, extracts financial details, categorizes expenses using rule-based logic and Multinomial Naive Bayes, stores records in SQLite, and displays spending summaries through a React dashboard.
